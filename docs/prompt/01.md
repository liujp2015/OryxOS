# OryxOS 对话提示词 · 01

> 本文件归档用户与 Claude Code 在 OryxOS 项目上的对话提示词历史。
> 文件命名按序号递增（`01.md` / `02.md` / ...），每段对话独立成文。

---

1. `@docs\`  理解一下这四个文档，这四个文档就是这个项目要做的事情
2. 整理一下这四个文档的格式，整理成md好看的格式
3. **（UI 选项回答）** 选了「清爽工程风 (推荐)」
4. 根据这些信息，生成 claude.md
5. CLAUDE.md 位置放错了
6. **（UI 选项回答）** 选了「根目录 CLAUDE.md」
7. 基于这些信息，生成 readme。readme 文件参考标准的开源项目来写。
8. README.md 和 CLAUDE.md 放到根目录
9. 那 4 个文件不动啊 你怎么都移到根目录了
10. Architecture 架构图用 svg 或 png 画，放到 docs/images
11. 用 maven 初始化这个项目，按照文档中的 9 个模块，并能通过编译打包
12. boot 能 java -jar 运行了吗？是一个 main 函数
13. 它是一个 web 项目 为什么没有 application.yml
14. oryxos 的 cli 需要有一个 main 函数，它要能运行，打印 oryxos 的版本信息
15. `@oryxos-channel-cli\` 这个模块 的 cli 需要有一个 main 函数，它要能运行，打印 oryxos 的版本信息
16. 是在 boot 项目启动的后面 加 --version 时 需要有一个 main 函数，它要能运行，打印 oryxos 的版本信息
17. 压缩一下上下文
18. 画一个 oryxos 的 logo，放在 readme，用 svg 格式，你自由发挥，我需要一个 logo
19. 这些内容，写入 `docs/prompt/01.md`，带上序号
20. `E:\\github\\mq9\\website` 理解一下这个项目代码。oryxos 要做一个一模一样的主页，你先理解下。看是否需要我做什么
21. 方案B
22. 全部用默认的
23. ok 开干
24. 继续
25. OWNER是项目名称吗？
26. liujp2015
27. https://github.com/liujp2015/OryxOS.git
28. 你要看是否要走代理 提交
29. http://127.0.0.1:7890
30. 本地启动 我看下效果
31. 你需要根据 /docs 的md文件 写出网站的文档 还有需要支持中英文切换
32. 不需要用这个skill 直接做
33. 把根目录的docs中图片目录中logo换一下 换到website使用
34. 首页的 Core Capabilities / The Three Pillars of OryxOS 太宽了 导致最下面出现滚动条 Core Capabilities 只要自己的部分出现滚动条 或者缩小 不要整个页面出现滚动条
35. github 上项目的 page 怎么配置
36. 已经部署了 但是js和css无法访问404 界面没有样式 https://liujp2015.github.io/OryxOS/
37. 那个README.md 没有上传到git
38. README.md 中引用的图片有问题 也上传到git
39. 提示词加到：docs/prompt/01.md 把这个01.md 改为加序号和提示词 不需要分类